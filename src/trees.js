class Trees {
    constructor(recursiveFunction) {
        this.recursiveFunction = recursiveFunction
        this.topLevel = this.recursiveFunction.topLevel 
    }
}

Trees.prototype.findChildren { 
    
}